<config>
  <param name="http.port">8081</param>
  <param name="appserver.url">http://riceweb:8081</param>
  <param name="application.host">http://riceweb:8081</param>

  <param name="plugin.dir">/usr/local/rice/plugins</param>

  <param name="datasource.username">rice</param>
  <param name="datasource.password">rice</param>
  <param name="datasource.ojb.platform">MySQL</param>
  <param name="datasource.platform">org.kuali.rice.core.framework.persistence.platform.MySQLDatabasePlatform</param>
  <param name="datasource.url">jdbc:mysql://mysql_kfs/rice</param>
  <param name="datasource.driver.name">com.mysql.jdbc.Driver</param>
  <param name="datasource.pool.validationQuery">select 1</param>

  <param name="attachment.dir.location">/usr/local/rice/kew_attachments</param>
  <param name="data.xml.root.location">/usr/local/rice/kew/xml</param>

  <!-- log4j settings -->
  <param name="log4j.settings.path">/usr/local/rice/log4j.properties</param>
  <param name="log4j.settings.reloadInterval">5</param>

  <!-- Keystore Configuration -->
  <param name="keystore.file">/usr/local/rice/rice.keystore</param>
  <param name="keystore.alias">rice</param>
  <param name="keystore.password">r1c3pw</param>

  <param name="mail.relay.server">localhost</param>
  <param name="mailing.list.batch">mailing.list.batch</param>
  <param name="encryption.key">7IC64w6kAAAA</param>

  <!-- Dummy Login Filter - use if you don't want to go through CAS -->
  <param name="filter.login.class">org.kuali.rice.krad.web.filter.DummyLoginFilter</param>
  <param name="filtermapping.login.1">/*</param> 

  <!-- Allow for single sign-out for CAS -->
  <!-- <param name="listener.logout.class">org.jasig.cas.client.sessiocccn.SingleSignOutHttpSessionListener</param> -->

  <param name="dev.mode">false</param>

  <param name="kfs.url">http://finweb.kuali.co:8080/kfs-dev</param>
</config>
